minikube start
minikube version
kubectl create deployment nginx-deploy --image=nginx
kubectl expose deployment nginx-deploy --type=NodePort --port=80
kubectl minikube service nginx-deploy --url
kubectl pods
kubectl describe pods
kubectl get svc

docker pull jsonrivers/nagios
docker pull jasonrivers/nagios
docker run --name nagios3 -p 9090:80 -d jasonrivers/nagios

in powershell
sudo apt update
sudo apt-get install docker.io
sudo apt install git
Sudo apt install nano
Sudo docker ps
in the file
git add .
git commit -m "first commit"
git branch -M main
git remote add origin http-link
git push -u origin 
again in powershell
git clone <copied http url>
ls
where the file is in that folder 
Nano Docker file
FROM nginx:alpine
COPY . /usr/share/nginx/html
sudo docker build –t mywebapp .
sudo docker run -d -p 80:80 mywebapp
then copy ipv4 address and run in chrome

docker pull redis
docker run --name my-redis -d redis
docker ps
docker exec -it my-redis redis-cli
127.0.0.1:6379> SET name "Alice"
OK
127.0.0.1:6379> GET name
"Alice"
127.0.0.1:6379>exit 
docker stop my-redis
docker start my-redis
docker rm my-redis  // Note before removing stop the container
docker rmi redis // remove redis image


pipeline {
    agent any
    tools{
        maven 'MAVEN_HOME'
    }
    stages {
        stage('git repo & clean') {
            steps {
                bat "rmdir  /s /q Maven_java || exit 0"
               bat "git clone https://github.com/Nikhitha710/Maven_java.git"
                bat "mvn clean -f Maven_java"
            }
        }
        stage('install') {
            steps {
                bat "mvn install -f Maven_java"
            }
        }
        stage('test') {
            steps {
                bat "mvn test -f Maven_java"
            }
        }
        stage('package') {
            steps {
                bat "mvn package -f Maven_java"
            }
        }
    }
}

